server {
    listen 80;
    server_name 38.244.144.223;  # Или IP сервера

    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ /index.html;
    }
}
